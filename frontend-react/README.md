# PlugGPT Frontend (React + Vite + Tailwind)

## Setup
```bash
cd frontend-react
npm install
npm run dev
# open http://localhost:5173
```
Make sure the backend is running on http://localhost:8000.
You can customize API URL in `src/ui/App.tsx`.
